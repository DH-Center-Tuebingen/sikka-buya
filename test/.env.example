#Used to add and remove the test database
DB_SUPER_NAME=postgres
DB_SUPER_USER=postgres
DB_SUPER_PASSWORD=

# Use a different user than in production
DB_READ_ONLY_USER=test_visitor
DB_READ_ONLY_PASSWORD=

# Prefilled for standard configuration. Just add your password.
DB_USER=postgres
DB_PASSWORD=
DB_PORT=5432
DB_HOST=localhost
DB_NAME=sikka-buya-test
MAX_SEARCH=20
JWT_SECRET=secret

# Defines where the frontend is located relative to the backend.
# Required for the backend to write files to the frontend directories. 
RELATIVE_FRONTEND_PUBLIC_LOCATION="../frontend/public"

# Optional: for recording cypress runs to a dashboard.
CYPRESS_RECORD_KEY=
